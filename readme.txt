# Introduction

This is Jenny's personal blog.

terminal command to run jekyll locally:

alias myblog='jekyll server --watch --baseurl ""'


# Instruction

1. open terminal, `cd ~/Document/Jenny`, `code .`;
2. in terminal, `myblog`, then in browser, `localhost:4000`